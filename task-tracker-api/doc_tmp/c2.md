# Module 4 CI Safety Review — `.github/workflows/ci.yml`

| Check | Pass/Concern | Evidence | Minimal fix |
|---|---|---|---|
| 1. Missing `push`/`pull_request` trigger | Pass | Lines 3–5: `on: push: / pull_request:` both present, no branch filters excluding anything | — |
| 2. Python version not pinned to 3.11 | Pass | Line 20: `python-version: "3.11"` explicit | — |
| 3. Installs deps but never runs pytest | Pass | Line 35: `run: pytest -v --tb=short` present after install step | — |
| 4. `continue-on-error` | Pass | Not present anywhere in the file | — |
| 5. `\|\| true` | Pass | Not present anywhere in the file | — |
| 6. `--exit-zero` | Pass | Not present anywhere in the file | — |
| 7. pytest output piped in a way that hides exit code | Pass | Line 35 runs `pytest -v --tb=short` as a bare command — no `\|`, `tee`, or subshell wrapping that would swallow `$?` | — |
| 8. Deployment steps that don't belong in this module | Pass | Job has only checkout → setup-python → cache → pip install → pytest; no publish/deploy/docker/release steps | — |
| 9. Workflow file in the wrong folder | Pass | File resides at `<repo-root>/.github/workflows/ci.yml` (`/home/esu-linux/AAC/.github/workflows/ci.yml`), which is the required location for GitHub Actions | — |

No issues found — file left unchanged, no edits made.

**Non-blocking observation** (not one of the 9 checks): the `Cache pip dependencies` step's cache key references `task-tracker-api/requirements.txt`, which is correct since `hashFiles()` resolves relative to the repo root, not the job's `working-directory`. Currently correct — worth re-checking if the app directory ever moves again.
