# Task Tracker API — Endpoint & Status-Transition Audit

Reviewed files: `app/main.py`, `app/models.py`.

Note upfront: `validate_status_transition` (imported at `app/main.py:25` from `app.business_rules`) is called but its body lives in `app/business_rules.py`, which was not in scope for this audit — so the actual transition rules are **not verifiable from these two files alone** and are marked accordingly.

## 1. Endpoints and status codes

| Claim | Evidence file/function | Confidence | What I should manually check |
|---|---|---|---|
| `GET /` returns 200 (implicit default), a dict with name/docs/health/tasks links | `app/main.py:53` `root()` | High | None — no error paths, plain dict return |
| `GET /json/version` returns 200 (implicit default), dict with name/version | `app/main.py:63` `json_version()` | High | None |
| `GET /health` returns 200 (implicit default), body = `HealthResponse` | `app/main.py:72` `health()` | High | `app/schemas.py` for `HealthResponse` shape (out of scope here) |
| `GET /tasks` returns 200 (implicit default), list of `TaskResponse`, filterable by `status`, `priority`, `tag` query params | `app/main.py:80` `list_tasks()` | High | `app/storage.py::get_all_tasks` for actual filter semantics |
| `POST /tasks` returns explicit 201, body = `TaskResponse` | `app/main.py:89` `create_task()`, `status_code=status.HTTP_201_CREATED` | High | `app/storage.py::add_task` for validation/id-generation not visible in main.py |
| `GET /tasks/{task_id}` returns 200 or 404 (`"Task with id {id} not found"`) | `app/main.py:94-102` `get_task()` | High | None — logic fully visible |
| `PATCH /tasks/{task_id}` returns 200, or 404 (task missing — checked twice, once pre-transition-check and once post-update), or 422 (`ValueError` from `storage.update_task`, message from exception) | `app/main.py:106-129` `update_task()` | High | Whether `validate_status_transition` itself raises an `HTTPException` (and what status code) — **[VERIFY]**, not defined in `main.py` or `models.py`, only imported from `app.business_rules` at `app/main.py:25` |
| `DELETE /tasks/{task_id}` returns explicit 204 (no body) or 404 if not deleted | `app/main.py:132-139` `delete_task()`, `status_code=status.HTTP_204_NO_CONTENT` | High | None |
| `POST /tasks/{task_id}/comments` returns explicit 201, or 422 (`ValueError` from `storage.add_comment`), or 404 if `comment is None` (task not found) | `app/main.py:142-161` `create_comment()` | High | If 422 or 404, check with rules in `app/storage.py::add_comment` |
| `GET /tasks/{task_id}/comments` returns 200, or 404 with two distinct messages depending on whether the task exists but has no comment vs task doesn't exist | `app/main.py:164-181` `read_comment()` | High | None — both branches visible via `storage.get_comment` returning `(exists, comment)` tuple |
| `PATCH /tasks/{task_id}/comments` returns 200 or 404 (`"Comment for task id {id} not found"`) | `app/main.py:184-196` `patch_comment()` | Medium | If 404 check with rules in `app/storage.py::update_comment` whether task-not-found or comment-not-found  |
| `PUT /tasks/{task_id}/comments` returns 200 or 404 (same message as PATCH) | `app/main.py:199-211` `put_comment()` | Medium | if 404 check with rules in `app/storage.py::replace_comment` |
| `DELETE /tasks/{task_id}/comments` returns explicit 204, or 404 (task not found), or 404 (comment not found) — two distinct checks | `app/main.py:214-230` `remove_comment()` | High | None — both branches visible |

## 2. Task status values and transition rules

| Claim | Evidence file/function | Confidence | What I should manually check |
|---|---|---|---|
| Three status values exist: `ToDo`, `InProgress`, `Done` | `app/models.py:36-39` `TaskStatus(str, Enum)` | High | None |
| Default status on task creation is `ToDo` | `app/models.py:82` `TaskCreate.status: TaskStatus = TaskStatus.TODO` | High | None |
| `TaskUpdate.status` is optional (`None` = no change requested) | `app/models.py:119` `TaskUpdate.status: Optional[TaskStatus] = None` | High | None |
| On `PATCH /tasks/{id}`, if `status` is provided, the API loads the existing task and calls `validate_status_transition(existing.status, payload.status)` before applying the update | `app/main.py:107-115` `update_task()` | High | None — call site is visible |
| **The actual set of allowed/forbidden transitions (e.g. can `ToDo` go directly to `Done`?) is NOT visible in `app/main.py` or `app/models.py`** | app/business_rules.py | High | Read `app/business_rules.py::validate_status_transition` directly — not in the two files reviewed |
| Whether `validate_status_transition` raises `HTTPException` itself, returns a bool, or raises a plain exception caught elsewhere | app/business_rules.py | High | read `app/business_rules.py`. Any transition not mentioned in the business_rules is not allowed|

**Bottom line:** endpoint shapes and status codes are fully evidenced by `app/main.py`; status *values* are evidenced by `app/models.py`; but the transition **rules** themselves are outside the two files reviewed and must be confirmed against `app/business_rules.py` before relying on them.
