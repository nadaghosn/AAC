# AI Help Summary — By Section

| Section | AI role | What was done |
|---|---|---|
| **CI** | Review only | Read `.github/workflows/ci.yml` directly (not just trusting commit messages) to write the README's CI summary; ran a dedicated dangerous-shortcuts check (`continue-on-error`, `\|\| true`, skipped pytest, vague Python version, missing dependency install) — none found. Did not author the workflow file itself. |
| **Docker** | Draft + review | Drafted the multi-stage `Dockerfile` design, `docker-run.sh` convenience script, and one-line comments before every instruction. Reviewed the built image for a real bug (`__pycache__` leaking in despite `.dockerignore`) and drafted the fix (`**/`-prefixed patterns). Verified non-root user, no baked secrets, and `/health` on every rebuild via live `docker build`/`docker run`, not just reading the Dockerfile. |
| **Docs** | Draft + review | Drafted Google-style docstrings (summary/Args/Returns/Raises/Example) for every route handler and public function, one-line comments above each, and a full README.md rewrite (setup/run/test/Docker/CI/structure/limitations). Then reviewed those same docs against live runtime behavior (`TestClient`, `/openapi.json`) and found/fixed 2 real mismatches (timestamp format, a missing test for a documented validation case), plus flagged one still-open gap (404s not declared in the OpenAPI schema). |
| **Security** | Review only | Verified the Docker image runs as non-root (`app`, uid 1000), contains no `.env`/`.git`/secrets, and is built from a fixed base image tag. Checked CI for shortcuts that could hide a real failure. No auth/authz was added or reviewed for implementation — explicitly out of scope per project constraints. |
| **Debugging** | Review → fix | Root-caused the `.dockerignore` pycache leak (Docker's non-recursive pattern matching) rather than guessing. Root-caused a README self-contradiction (44 vs 45 test count) by diffing the file against itself. Verified an edge case (`PATCH` with `tags: null`) live with `TestClient` before documenting it, rather than asserting behavior from reading code alone. |
