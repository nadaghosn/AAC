# Task Tracker repository summary

| Number ID | Claim | Evidence file | Evidence summary | Confidence | Assumption to verify |
|---:|---|---|---|---|---|
| 1 | The repo implements a task-management REST API with task CRUD, task filters, and task-comment endpoints. | `/home/esu-linux/AAC/task-tracker-api/app/main.py` | Route decorators define `GET/POST /tasks`, `GET/PATCH/DELETE /tasks/{task_id}`, filters for `status`, `priority`, and `tag`, plus comment routes under `/tasks/{task_id}/comments`. | High | Verify endpoint behavior through the test suite or a running instance. |
| 2 | Task data is process-local and non-persistent. | `/home/esu-linux/AAC/task-tracker-api/app/storage.py` | Storage is the module-level dictionary `_tasks: dict[str, TaskResponse] = {}`; add, update, and delete functions operate on it directly. | High | Verify no external startup code repopulates this dictionary. |
| 3 | The API validates and normalizes task inputs. | `/home/esu-linux/AAC/task-tracker-api/app/models.py` | `TaskCreate` forbids extra fields; titles are stripped, required, non-blank, and capped at 200 characters; tags are stripped, lowercased, de-duplicated, and blank values dropped. | High | Verify clients receive the expected validation-error format. |
| 4 | Workflow status changes are deliberately constrained. | `/home/esu-linux/AAC/task-tracker-api/app/business_rules.py`, `/home/esu-linux/AAC/task-tracker-api/app/main.py` | `VALID_TRANSITIONS` permits `ToDo → InProgress`, `InProgress → Done`, `Done → InProgress`, and unchanged status; the task PATCH handler calls the validator before storage update. | High | Verify the intended product workflow includes no additional transitions. |
| 5 | A single-file browser Kanban UI consumes the API and organizes tasks into three status columns. | `/home/esu-linux/AAC/task-tracker-api/frontend/index.html` | The HTML declares `ToDo`, `InProgress`, and `Done` columns; its JavaScript fetches `/tasks`, filters by tag, and uses POST/PATCH/DELETE requests for task actions. | High | Verify the configured API base URL matches the deployment environment. |
