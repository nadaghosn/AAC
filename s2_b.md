# Manual Edits to s2.md — Summary of 5 Verified Claims

This summarizes exactly what you changed by hand in `s2.md` for the 5 claims you flagged as manually verified. Diffed against the version I originally wrote and saved.

| Claim | Original entry (as I wrote it) | Your edit | What changed |
|---|---|---|---|
| `POST /tasks/{task_id}/comments` — 422 vs 404 conditions | Confidence: High. Check note: *"`app/storage.py::add_comment` — what condition raises `ValueError` vs returns `None` (...) is not distinguishable from `main.py` alone — **[VERIFY]**"* | Check note replaced with: *"If 422 or 404, check with rules in `app/storage.py::add_comment`"* | Removed the `[VERIFY]` flag and the "not distinguishable from main.py alone" caveat; reworded as a conditional check pointer. Confidence unchanged (High). |
| `PATCH /tasks/{task_id}/comments` — 404 ambiguity | Confidence: Medium. Check note: *"This 404 message doesn't distinguish "task not found" from "comment not found" (...) — **[VERIFY]** in `app/storage.py::update_comment` whether task-not-found and comment-not-found are actually distinguished internally"* | Check note replaced with: *"If 404 check with rules in `app/storage.py::update_comment` whether task-not-found or comment-not-found"* | Removed the `[VERIFY]` flag; condensed wording. Confidence unchanged (Medium). |
| `PUT /tasks/{task_id}/comments` — 404 ambiguity | Confidence: Medium. Check note: *"Same as above — **[VERIFY]** in `app/storage.py::replace_comment`"* | Check note replaced with: *"if 404 check with rules in `app/storage.py::replace_comment`"* | Removed the `[VERIFY]` flag; condensed wording. Confidence unchanged (Medium). |
| Allowed/forbidden status-transition set | Evidence: `—`. Confidence: **[VERIFY]**. Check note: *"Read `app/business_rules.py::validate_status_transition` directly — not in the two files reviewed"* | Evidence changed to: `app/business_rules.py`. Confidence changed to: **High**. Check note: unchanged text kept as-is | Upgraded confidence from `[VERIFY]` to `High` and added a concrete evidence source (`app/business_rules.py`), indicating you read the file and confirmed the transition set. |
| `validate_status_transition` raising `HTTPException` behavior | Evidence: `—`. Confidence: **[VERIFY]**. Check note: *"Same — read `app/business_rules.py`; `main.py` calls it with no surrounding try/except, implying it likely raises `HTTPException` directly, but this is inference, not evidence"* | Evidence changed to: `app/business_rules.py`. Confidence changed to: **High**. Check note replaced with: *"read `app/business_rules.py`. Any transition not mentioned in the business_rules is not allowed"* | Upgraded confidence from `[VERIFY]` to `High`, added evidence source, and added a new confirmed fact: **transitions not explicitly listed in `business_rules.py` are rejected by default** (deny-by-default behavior). |

## Net effect

- 3 comment-endpoint claims: `[VERIFY]` markers removed, notes reworded as conditional guidance — confidence levels (High / Medium / Medium) left as originally assessed.
- 2 status-transition claims: upgraded from `[VERIFY]` to `High` confidence with `app/business_rules.py` cited as evidence, and one new fact recorded — invalid/unlisted transitions are rejected by default.
